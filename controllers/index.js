'use strict';
const adminRoutes = require('./admin');
const beginnerRoutes = require('./beginner');
const documentationRoutes = require('./documentation');

module.exports = {
  adminRoutes,
  beginnerRoutes,
  documentationRoutes
};